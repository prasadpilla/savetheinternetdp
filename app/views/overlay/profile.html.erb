<p>Publish this image and set it as your profile picture !</p>
<p>&nbsp;</p>
<center>
<div class="row">
  <% @overlay_images.each do |image|  %>
    <div class="col-md-3 col-sm-6 col-xs-12 pdbtm-sm">
      <p><%= image_tag "https://s3-ap-southeast-1.amazonaws.com/saveinternet/#{current_user.uid}_#{image}",
              class: 'img-responsive',data: {action: 'zoom'} %></p>
      <div class="row">
        <div class="col-md-16 col-sm-16">
          <%= link_to 'Publish to Facebook', publish_path(image: image.split('.').first),
              class: 'btn btn-primary btn-block', target: '_blank' %>
        </div>
      </div>
    </div>
  <% end %>
</div>
</center>